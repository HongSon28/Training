#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;
 
int main(int argc, char *argv[]) {
    registerValidation(argc, argv);
    int n = inf.readInt(1, (int)1e5, "n");
    inf.readEoln();
    for (int i=1;i<=n;i++) {
        int x = inf.readInt(1,(int)1e6,"x");
        inf.readSpace();
        int k = inf.readInt(1,(int)1e9,"k");
        inf.readEoln();
    }
    inf.readEof();
}
